machine:
  php:
    version: 5.5.11
dependencies:
  override:
    - composer require typo3/surf:@dev
test:
  override:
    - echo "No tests to run"
deployment:
  production:
    branch: master
    commands:
      - ./flow surf:deploy Sfi:
          timeout: 900
